#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main()
{

    int n;

    // cin>>n;

    // scanf("%d", &n);

    // int a[n];

    // for(int i=0;i<n;i++){
    //     // cin>>a[i];

    //     scanf("%d", &a[i]);
    // }

    // for (int i = 0; i < sizeof(a); i++)
    // {
    //     /* code */
    // }

    int arr[] = {
        1,
        2,
        4,
        5,
    };
    printf("%d",sizeof(arr));

    /* Enter your code here. Read input from STDIN. Print output to STDOUT */
    return 0;
}
